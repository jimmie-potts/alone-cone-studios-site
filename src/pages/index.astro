---
import MuralGallery from '../components/MuralGallery.astro';
import { muralGallery, muralVideos } from '../data/murals';
import '../styles/global.css';

const formAction = 'https://formspree.io/p/3040768964113203164/f/mural-inquiry';
const featuredImage = {
  src: '/images/murals/otter-boops-mural-2026-001.jpeg',
  alt: 'A colorful mural of playful purple otters surrounded by bright green leaves.',
};
const thankYouImage = {
  src: '/images/murals/murray-the-octopus-mural-2023-001.JPG',
  alt: 'A cheerful yellow octopus mural surrounded by bright underwater plants.',
};

const trustedByPlaceholders = ['Local Shop', 'Community Space', 'Creative Studio', 'Small Business'];

const reviewPlaceholders = [
  {
    quote: 'Placeholder review: add a real client quote here before publishing.',
    name: 'Client Name',
  },
  {
    quote: "Placeholder review: use Minnie's real mural feedback when available.",
    name: 'Client Name',
  },
  {
    quote: 'Placeholder review: keep this section accurate and client-approved.',
    name: 'Client Name',
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta
      name="description"
      content="Minnie's mural portfolio, custom mural inquiries, and 2026 winter holiday window painting waitlist."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap"
      rel="stylesheet"
    />
    <title>Minnie Art | Custom Murals</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>

    <header class="site-header">
      <nav class="site-nav" aria-label="Primary navigation">
        <a href="#top" class="brand-mark">Alone Cone Studios <span>LLC</span></a>
        <div class="nav-actions">
          <a href="#murals">Murals</a>
          <a href="#contact">Inquiry</a>
        </div>
      </nav>
    </header>

    <main id="main">
      <section id="top" class="hero-section" aria-labelledby="hero-heading">
        <img
          src="/images/murals/minnie-paint-leaf.PNG"
          alt="Minnie hand painting leafy details on a large outdoor mural."
          width="1200"
          height="900"
          class="hero-image"
          loading="eager"
        />
        <div class="hero-overlay"></div>
        <div class="hero-content">
          <p class="eyebrow">Florida Based. Will Travel.</p>
          <h1 id="hero-heading">Murals.</h1>
          <a href="#contact" class="hero-cta">Now Scheduling Fall 2026. Starting at $500.00</a>
        </div>
      </section>

      <section id="portfolio" class="feature-section" aria-labelledby="feature-heading">
        <div class="section-shell feature-grid">
          <div>
            <p class="section-label">Featured mural</p>
            <h2 id="feature-heading">One big wall, one colorful world.</h2>
            <p class="feature-copy">
              Hand-painted, one-of-a-kind murals that turn everyday walls into colorful little worlds.
            </p>
          </div>
          <figure class="feature-photo">
            <img
              src={featuredImage.src}
              alt={featuredImage.alt}
              width="1200"
              height="900"
              loading="lazy"
            />
            <figcaption>Otter Boops mural · 2026</figcaption>
          </figure>
        </div>
      </section>

      <section class="trusted-section" aria-labelledby="trusted-heading">
        <div class="section-shell">
          <p class="section-label">Trusted by</p>
          <h2 id="trusted-heading">Client logos coming soon.</h2>
          <div class="logo-row" aria-label="Placeholder client logos">
            {trustedByPlaceholders.map((name) => (
              <div class="logo-pill">
                <span>{name}</span>
                <small>Placeholder</small>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section class="reviews-section" aria-labelledby="reviews-heading">
        <img class="reviews-background" src="/images/murals/Bouquet-mural-2017-001.jpeg" alt="" loading="lazy" />
        <div class="reviews-overlay"></div>
        <div class="section-shell">
          <p class="section-label">Reviews</p>
          <h2 id="reviews-heading">Happy Clients.</h2>
          <div class="review-grid">
            {reviewPlaceholders.map((review) => (
              <figure class="review-card">
                <blockquote>{review.quote}</blockquote>
                <figcaption>{review.name} · Placeholder</figcaption>
              </figure>
            ))}
          </div>
        </div>
      </section>

      <section class="windows-section" aria-labelledby="windows-heading">
        <div class="section-shell windows-card">
          <div>
            <p class="section-label">Coming soon</p>
            <h2 id="windows-heading">2026 winter holiday windows.</h2>
          </div>
          <p>
            Holiday window painting is opening soon. Join the 2026 winter waitlist for storefronts, businesses, homes, and seasonal spaces.
          </p>
          <a href="#contact" class="text-link">Join the waitlist</a>
        </div>
      </section>

      <section id="contact" class="contact-section" aria-labelledby="contact-heading">
        <div class="section-shell contact-grid">
          <div>
            <p class="section-label">Inquiry form</p>
            <h2 id="contact-heading">Let’s Schedule Your Project.</h2>
            <p>
              Share a few details and Minnie will follow up by email.
            </p>
            <img
              class="contact-artwork"
              src="/images/murals/cbs-mural-2026-004.jpeg"
              alt="A hand-painted CBS Collision Body Services mural on large black doors."
              width="2625"
              height="2576"
              loading="lazy"
            />
            <div id="thanks" class="thanks-card" hidden>
              <img
                src={thankYouImage.src}
                alt="Fun placeholder thank-you image for Minnie's inquiry form."
                width="1200"
                height="900"
                loading="lazy"
              />
              <p>Thank you! Minnie will read your note soon.</p>
            </div>
          </div>

          <form class="intake-form" action={formAction} method="POST" data-intake-form>
            <input type="hidden" name="_subject" value="New Minnie mural inquiry" />

            <label>
              Name
              <input name="name" type="text" autocomplete="name" required />
            </label>

            <label>
              Email
              <input name="email" type="email" autocomplete="email" required />
            </label>

            <label class="full-span">
              Project description / questions
              <textarea name="project_description" rows="5" required></textarea>
            </label>

            <label>
              Square footage (Ex. 10ft wide × 10ft tall wall = 100 SFT) — best guess
              <input name="square_footage" type="text" inputmode="decimal" />
            </label>

            <fieldset>
              <legend>Project setting</legend>
              <label><input type="radio" name="setting" value="Indoor" /> Indoor</label>
              <label><input type="radio" name="setting" value="Outdoor" /> Outdoor</label>
            </fieldset>

            <fieldset>
              <legend>Project type</legend>
              <label><input type="radio" name="project_type" value="Business" /> Business</label>
              <label><input type="radio" name="project_type" value="Residential" /> Residential</label>
              <label><input type="radio" name="project_type" value="Other" /> Other</label>
            </fieldset>

            <label>
              Budget
              <select name="budget">
                <option value="">Select a range</option>
                <option value="$500-$1,000">$500-$1,000</option>
                <option value="$1k-$3k">$1k-$3k</option>
                <option value="$3k-$5k">$3k-$5k</option>
                <option value="$5k+">$5k+</option>
              </select>
            </label>

            <label class="checkbox-row full-span">
              <input
                type="checkbox"
                name="holiday_window_waitlist"
                value="Yes, add me to the 2026 winter holiday window painting waitlist"
              />
              Add me to the 2026 winter holiday window painting waitlist.
            </label>

            <p class="form-error full-span" data-form-error hidden role="alert">
              Something went sideways. Please try again or email Minnie directly.
            </p>

            <button class="submit-button full-span" type="submit" data-submit-button>Submit inquiry</button>
          </form>
        </div>
      </section>

      <MuralGallery images={muralGallery} videos={muralVideos} />
    </main>

    <script>
      const params = new URLSearchParams(window.location.search);
      const thanks = document.querySelector('#thanks');
      if (params.get('thanks') === 'true' && thanks instanceof HTMLElement) {
        thanks.hidden = false;
      }

      const intakeForm = document.querySelector('[data-intake-form]');
      if (intakeForm instanceof HTMLFormElement && thanks instanceof HTMLElement) {
        const errorMessage = intakeForm.querySelector('[data-form-error]');
        const submitButton = intakeForm.querySelector('[data-submit-button]');

        intakeForm.addEventListener('submit', async (event) => {
          event.preventDefault();

          if (errorMessage instanceof HTMLElement) {
            errorMessage.hidden = true;
          }

          if (submitButton instanceof HTMLButtonElement) {
            submitButton.disabled = true;
            submitButton.textContent = 'Sending...';
          }

          try {
            const response = await fetch(intakeForm.action, {
              method: 'POST',
              body: new FormData(intakeForm),
              headers: {
                Accept: 'application/json',
              },
            });

            if (!response.ok) {
              throw new Error('Form submission failed');
            }

            intakeForm.reset();
            thanks.hidden = false;
            thanks.scrollIntoView({ behavior: 'smooth', block: 'center' });
          } catch {
            if (errorMessage instanceof HTMLElement) {
              errorMessage.hidden = false;
            }
          } finally {
            if (submitButton instanceof HTMLButtonElement) {
              submitButton.disabled = false;
              submitButton.textContent = 'Submit inquiry';
            }
          }
        });
      }

    </script>
  </body>
</html>
